{
  "process": [
    {
      "process_id": "create_table",
      "overwrite": false,
      "delete": false,
      "parameters": {
        "schema": "observation_utility",
        "table": "analysis_method_translate",
        "command": [
          "id SERIAL",
          "species_id INTEGER REFERENCES observation_utility.species (id)",
          "src_analysis_method_id INTEGER REFERENCES observation_utility.analysis_method (id)",
          "dst_analysis_method_id INTEGER REFERENCES observation_utility.analysis_method (id)",
          "factor DOUBLE PRECISION",
          "addon DOUBLE PRECISION",
          "exponent DOUBLE PRECISION DEFAULT 1",
          "UNIQUE (species_id, src_analysis_method_id, dst_analysis_method_id)",
          "PRIMARY KEY (id)"
        ]
      },
      "audit": {
        "INSERT": true,
        "UPDATE": true,
        "DELETE": true
      }
    }
  ]
}
