{
  "process": [
    {
      "process_id": "create_table",
      "overwrite": false,
      "delete": false,
      "parameters": {
        "schema": "landscape",
        "table": "landuse_observation",
        "command": [
          "id SERIAL",
          "sampling_log_id INTEGER REFERENCES observation.sampling_log (id)",
          "geolocation_id INTEGER REFERENCES observation.geolocation (id)",
          "landuse_genus_id INTEGER REFERENCES landscape_utility.landuse_genus",
          "observed_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP",
          "UNIQUE (geolocation_id, observed_at)",
          "PRIMARY KEY (id)"
        ]
      },
      "audit": {
        "INSERT": true,
        "UPDATE": true,
        "DELETE": true
      }
    },
    {
      "process_id": "create_table",
      "overwrite": false,
      "delete": false,
      "parameters": {
        "schema": "landscape",
        "table": "landcover_observation",
        "command": [
          "id SERIAL",
          "sampling_log_id INTEGER REFERENCES observation.sampling_log (id)",
          "geolocation_id INTEGER REFERENCES observation.geolocation (id)",
          "landcover_genus_id INTEGER REFERENCES landscape_utility.landuse_genus",
          "observed_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP",
          "UNIQUE (geolocation_id, observed_at)",
          "PRIMARY KEY (id)"
        ]
      },
      "audit": {
        "INSERT": true,
        "UPDATE": true,
        "DELETE": true
      }
    }
  ]
}
