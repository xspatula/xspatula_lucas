{
  "process": [
    {
      "process_id": "create_table",
      "overwrite": false,
      "delete": false,
      "parameters": {
        "schema": "observation_utility",
        "table": "edna_bioinformatics",
        "command": [
          "id SERIAL",
          "name TEXT UNIQUE",
          "analysis_method_id INTEGER REFERENCES observation_utility.analysis_method (id)",
          "taxa_id INTEGER REFERENCES observation_utility.taxa (id)",
          "taxa_function_id INTEGER REFERENCES observation_utility.taxa_function (id)",
          "abundance FLOAT",
          "abundance_unit INTEGER REFERENCES observation_utility.unit (id)",
          "abstract TEXT",
          "created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP(1)",
          "UNIQUE (analysis_method_id,taxa_id,taxa_function_id)",
          "PRIMARY KEY (id)"
        ]
      },
      "audit": {
        "INSERT": true,
        "UPDATE": true,
        "DELETE": true
      }
    }
  ]
}
