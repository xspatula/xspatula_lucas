{
  "process": [
    {
      "process_id": "create_table",
      "overwrite": false,
      "delete": false,
      "parameters": {
        "schema": "observation_utility",
        "table": "indicator",
        "command": [
          "id SERIAL",
          "quantity_id INTEGER REFERENCES observation_utility.quantity (id)",
          "name TEXT UNIQUE",
          "alias TEXT UNIQUE",
          "display_name TEXT",
          "abstract TEXT",
          "PRIMARY KEY (id)"
        ]
      },
      "audit": {
        "INSERT": true,
        "UPDATE": true,
        "DELETE": true
      }
    },
    {
      "process_id": "create_table",
      "overwrite": false,
      "delete": false,
      "parameters": {
        "schema": "observation_utility",
        "table": "indicator_parity",
        "command": [
          "id SERIAL",
          "src_indicator_id INTEGER REFERENCES observation_utility.indicator (id)",
          "dst_indicator_id INTEGER REFERENCES observation_utility.indicator (id)",
          "UNIQUE (src_indicator_id, dst_indicator_id)",
          "PRIMARY KEY (id)"
        ]
      },
      "audit": {
        "INSERT": true,
        "UPDATE": true,
        "DELETE": true
      }
    }
  ]
}
