{
  "process": [
    {
      "process_id": "create_table",
      "overwrite": false,
      "delete": false,
      "parameters": {
        "schema": "observation_utility",
        "table": "taxa",
        "command": [
          "id SERIAL",
          "name TEXT UNIQUE",
          "latin_name TEXT UNIQUE",
          "taxa_level_id INTEGER REFERENCES observation_utility.taxa_level (id)",
          "taxa_status_id INTEGER REFERENCES observation_utility.taxa_status (id)",
          "updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP(1)",
          "PRIMARY KEY (id)"
        ]
      },
      "audit": {
        "INSERT": true,
        "UPDATE": true,
        "DELETE": true
      }
    },
    {
      "process_id": "create_table",
      "overwrite": false,
      "delete": false,
      "parameters": {
        "schema": "observation_utility",
        "table": "taxa_parity",
        "command": [
          "id SERIAL",
          "src_taxa_id INTEGER REFERENCES observation_utility.taxa (id)",
          "dst_taxa_id INTEGER REFERENCES observation_utility.taxa (id)",
          "UNIQUE (src_taxa_id, dst_taxa_id)",
          "PRIMARY KEY (id)"
        ]
      },
      "audit": {
        "INSERT": true,
        "UPDATE": true,
        "DELETE": true
      }
    }
  ]
}
