{
  "process": [
    {
      "process": "add_process",
      "overwrite": false,
      "parameters": {
        "root_process": "machine_learning",
        "process": "spectra_indicator_permutation_selection",
        "min_user_stratum": 3,
        "title": "Spectra indicator permutation selection",
        "label": "Select spectra for specific indicator and model using univariate selection."
      },
      "nodes": [
        {
          "parent": "process",
          "element": "parameters",
          "parameter": [
            {
              "parameter": "project_root_fp",
              "parameter_type": "text",
              "required": false,
              "default_value": "",
              "hint": "The root fp of the project"
            },
            {
              "parameter": "dataframe",
              "parameter_type": "text",
              "required": false,
              "default_value": "raw",
              "hint": "The dataframe to use as input, raw = original spectra, previous = the latest saved parquet file(s), or state the name of the parquet file with spectra derived from earlier step(s) to use as input."
            },
            {
              "parameter": "indicator_array",
              "parameter_type": "array",
              "required": false,
              "default_value": "",
              "hint": "Comma separated list of the indicators to use for univariate selection"
            },
            {
              "parameter": "selector_array",
              "parameter_type": "array",
              "required": true,
              "default_value": "",
              "hint": "Comma separated list of the indicators to use for univariate selection, valid entries: permutation, rfe,tree_based"
            },
            {
              "parameter": "regressor_array",
              "parameter_type": "array",
              "required": true,
              "default_value": "",
              "hint": "Comma separated list of the regressor to apply, valid entries: ols, theilsen, huber, knn, detree, svr, mlp, cubist, plsr."
            },

            {
              "parameter": "selector_parameters",
              "parameter_type": "text",
              "required": false,
              "default_value": "default",
              "hint": "The parameterisation for each selector if applied, if not set to default you have to give the relative or absolute path."
            },
            {
              "parameter": "separate_selections",
              "parameter_type": "boolean",
              "required": false,
              "default_value": false,
              "hint": "If the selection should be done sepately for each indicator, false = all indicators in one selection (single dataframe output); true= individual dataframes will be created for each indicator."
            },
            {
              "parameter": "model_parameters_fp",
              "parameter_type": "text",
              "required": false,
              "default_value": "default",
              "hint": "Relative or absolute path to the JSON file with regressor hyperparameters. If not set, uses the package default."
            },
            {
              "parameter": "n_top_features_in_plot",
              "parameter_type": "integer",
              "required": false,
              "default_value": 0,
              "hint": "Maximum number of features to show in the selector bar plot. 0 = show all. Y-axis labels are suppressed when showing more than 40 features."
            },

            {
              "parameter": "colormap",
              "parameter_type": "text",
              "required": false,
              "default_value": "jet",
              "hint": "Name of the matplotlib colormap to use"
            }
          ]
        }
      ]
    }
  ]
}
