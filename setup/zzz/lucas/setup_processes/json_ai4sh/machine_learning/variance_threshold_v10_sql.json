{
  "process": [
    {
      "process": "add_process",
      "overwrite": false,
      "parameters": {
        "root_process": "machine_learning",
        "process": "select_variance_threshold",
        "min_user_stratum": 3,
        "title": "Select spectra variance threshold",
        "label": "Select and remove spectral bands with variance below a given threshold after scaling to reduce the number of input bands in the Machine Learning modelling: Available scalers: minmax, standard and robust."
      },
      "nodes": [
        {
          "parent": "process",
          "element": "parameters",
          "parameter": [
            {
              "parameter": "project_root_fp",
              "parameter_type": "text",
              "required": false,
              "default_value": "",
              "hint": "The root fp of the project"
            },
            {
              "parameter": "dataframe",
              "parameter_type": "text",
              "required": false,
              "default_value": "raw",
              "hint": "The dataframe to use as input, raw = original spectra, previous = the latest saved parquet file(s), or state the name of the parquet file with spectra derived from earlier step(s) to use as input."
            },
            {
              "parameter": "scaler",
              "parameter_type": "text",
              "required": false,
              "default_value": "none",
              "hint": "Scaler to apply before detecting variance, available options: minmax, standard and robust. If set to none, no variance threshold selection will be applied."
            },
            {
              "parameter": "threshold",
              "parameter_type": "float",
              "required": false,
              "default_value": 0.025,
              "hint": "The threshold to set for detecting spectral bands with low variance. If set to 0, no variance threshold selection will be applied"
            }
          ]
        }
      ]
    }
  ]
}
