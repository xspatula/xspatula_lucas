{
  "process": [
    {
      "process": "add_process",
      "overwrite": false,
      "parameters": {
        "root_process": "machine_learning",
        "process": "spectra_indicator_univariate_selection",
        "min_user_stratum": 3,
        "title": "Spectra indicator univariate selection",
        "label": "Select spectra for specific indicator using univariate selection."
      },
      "nodes": [
        {
          "parent": "process",
          "element": "parameters",
          "parameter": [
            {
              "parameter": "project_root_fp",
              "parameter_type": "text",
              "required": false,
              "default_value": "",
              "hint": "The root fp of the project"
            },
            {
              "parameter": "dataframe",
              "parameter_type": "text",
              "required": false,
              "default_value": "raw",
              "hint": "The dataframe to use as input, raw = original spectra, previous = the latest saved parquet file(s), or state the name of the parquet file with spectra derived from earlier step(s) to use as input."
            },
            {
              "parameter": "indicator_array",
              "parameter_type": "array",
              "required": false,
              "default_value": "",
              "hint": "Comma separated list of the indicators to use for univariate selection"
            },
            {
              "parameter": "separate_selections",
              "parameter_type": "boolean",
              "required": false,
              "default_value": false,
              "hint": "If the selection should be done sepately for each indicator, false = all indicators in one selection (single dataframe output); true= individual dataframes will be created for each indicator."
            },
            {
              "parameter": "univariate_selector_fp",
              "parameter_type": "text",
              "required": true,
              "default_value": "none",
              "hint": "The relative or absolute path to the univariate selector to apply, if set to none, no ward clustering will be applied even if the process is called. You have to define the ward clustering to apply and set the relative or absolute path to the ward_clustering.JSON file as the parameter."
            },
            {
              "parameter": "colormap",
              "parameter_type": "text",
              "required": false,
              "default_value": "jet",
              "hint": "Name of the matplotlib colormap to use"
            }
          ]
        }
      ]
    }
  ]
}
